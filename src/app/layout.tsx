import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "CodeCraft - Tech Consulting & Development Services",
  description: "Expert tech consulting, process automation, legacy modernization, and end-to-end development solutions. Transform your ideas into production-ready applications.",
  keywords: "tech consulting, software development, automation, modernization, Next.js",
  authors: [{ name: "CodeCraft" }],
  icons: {
    icon: "/logo.png",
    shortcut: "/logo.png",
    apple: "/logo.png",
  },
  openGraph: {
    title: "CodeCraft - Transform Ideas Into Production-Ready Code",
    description: "Expert consulting and development services for startups and enterprises.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
